# Internship Management System (ระบบจัดการข้อมูลนักศึกษาฝึกงาน)

ระบบจัดการข้อมูลและติดตามความคืบหน้านักศึกษาฝึกงาน พัฒนาด้วย Python และ PySide6

## คู่มือการใช้งาน (User Manual)
ดูรายละเอียดวิธีการใช้งานอย่างละเอียดได้ที่ไฟล์ [USER_MANUAL.md](USER_MANUAL.md)

## โครงสร้างโปรเจกต์ (Project Structure)

โปรเจกต์นี้ถูกจัดโครงสร้างแบบ Modular เพื่อให้ง่ายต่อการดูแลรักษาและแก้ไข โดยแบ่งไฟล์ตามหน้าที่การทำงานดังนี้:

### 📂 Root Directory

- **`main.py`**
  - **หน้าที่**: เป็นจุดเริ่มต้นของโปรแกรม (Entry Point)
  - **การทำงาน**: ทำหน้าที่สร้าง `QApplication` และเรียกใช้งาน `MainWindow` เพื่อเริ่มรันโปรแกรม

- **`constants.py`**
  - **หน้าที่**: เก็บค่าคงที่และการตั้งค่าต่างๆ ของระบบ
  - **ข้อมูลภายใน**:
    - `DEFAULT_FILENAME`: ชื่อไฟล์ฐานข้อมูลเริ่มต้น
    - `PROGRESS_ITEMS`: รายการงานที่ต้องติดตาม (เช่น รายงาน 2 เดือน, นิเทศ, โครงงาน)
    - `GRADES`: ตัวเลือกเกรด (A, B, C, etc.)
    - `MODERN_STYLESHEET`: โค้ด CSS สำหรับตกแต่งหน้าตาโปรแกรม

- **`utils.py`**
  - **หน้าที่**: เก็บฟังก์ชันช่วยเหลือ (Helper Functions) ที่ใช้ร่วมกันหลายจุด
  - **ฟังก์ชัน**: `create_card()` สำหรับสร้าง Frame ที่มีพื้นหลังสีขาวและขอบมน (Card UI)

- **`pdf_handler.py`**
  - **Class `PDFHandler`**: จัดการการสร้างไฟล์ PDF
  - **หน้าที่**: รับข้อมูลนักศึกษา (List of Records) และสร้างไฟล์ PDF รายงานสรุปผล
  - **การทำงาน**: สร้าง HTML Table จากข้อมูล และใช้ `QPrinter` แปลงเป็น PDF

- **`json_to_pdf.py`**
  - **หน้าที่**: สคริปต์แยกสำหรับแปลงไฟล์ JSON เป็น PDF โดยไม่ต้องเปิดหน้าจอหลัก
  - **การใช้งาน**: รันไฟล์นี้เพื่อสร้างรายงาน PDF จากฐานข้อมูล `db_students.json` ทันที

- **`USER_MANUAL.md`**
  - **หน้าที่**: คู่มือการใช้งานสำหรับผู้ใช้ (End User) อธิบายขั้นตอนการติดตั้งและการใช้งานฟีเจอร์ต่างๆ

### 📂 ui/ Directory (User Interface)

โฟลเดอร์นี้เก็บไฟล์ที่เกี่ยวข้องกับหน้าจอและการแสดงผลทั้งหมด

- **`ui/main_window.py`**
  - **Class `MainWindow`**: หน้าต่างหลักของโปรแกรม
    - จัดการ Menu Bar (File, Export) และ Toolbar
    - **Keyboard Shortcuts**: รองรับคีย์ลัด เช่น `Ctrl+N` (สร้างไฟล์), `Ctrl+S` (บันทึก), `Ctrl+O` (เปิดไฟล์)
    - จัดการการเปลี่ยนหน้า (StackWidget) ระหว่างหน้า "รายการ" และหน้า "บันทึกความคืบหน้า"
    - ควบคุมการโหลดและบันทึกไฟล์ JSON
    - ควบคุมฟังก์ชัน Export CSV และ PDF

- **`ui/management_tab.py`**
  - **Class `ManagementTab`**: แท็บ "จัดการข้อมูล"
    - แสดงรายชื่อนักศึกษาทางด้านซ้าย
    - แสดงรายละเอียดของนักศึกษาที่เลือกทางด้านขวา
    - มีปุ่ม เพิ่ม, แก้ไข, ลบ และปุ่ม "บันทึก/ติดตามงาน" เพื่อไปยังหน้า ProgressPage

- **`ui/overview_tab.py`**
  - **Class `OverviewTab`**: แท็บ "ภาพรวมความคืบหน้า"
    - แสดงตารางสรุปข้อมูลนักศึกษาทุกคนพร้อมสถานะงานแต่ละชิ้น
    - **Color Coding**: แสดงสีเกรด (เขียว=A/B, แดง=F) เพื่อให้ดูง่าย
    - สามารถดับเบิ้ลคลิกที่ช่องตารางเพื่อดูรายละเอียด (Popup) ของงานชิ้นนั้นๆ ได้

- **`ui/progress_page.py`**
  - **Class `ProgressPage`**: หน้า "บันทึกการติดตามงาน"
    - เป็นหน้าจอแยกสำหรับลงรายละเอียดความคืบหน้าของนักศึกษาแต่ละคน
    - **Smart Fields**: ช่องกรอกเกรดและหมายเหตุจะถูกปิด (Disabled) หากยังไม่ได้ติ๊กงานเสร็จ
    - มี Checkbox สำหรับติ๊กงานที่เสร็จแล้ว
    - มี Dropdown สำหรับให้เกรด และช่องกรอกหมายเหตุ (Note)

- **`ui/dialogs.py`**
  - **Class `AddStudentDialog`**: หน้าต่าง Popup สำหรับกรอกข้อมูล
    - ใช้สำหรับทั้งการ "เพิ่ม" นักศึกษาใหม่ และ "แก้ไข" ข้อมูลเดิม
    - มีฟอร์มกรอก รหัสนักศึกษา, ชื่อ, สถานที่ฝึกงาน, แผนก ฯลฯ
