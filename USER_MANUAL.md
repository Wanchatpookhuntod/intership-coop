# คู่มือการใช้งานระบบจัดการข้อมูลนักศึกษาฝึกงาน (User Manual)

## 0. การติดตั้ง (Installation)
ก่อนใช้งานโปรแกรม จำเป็นต้องติดตั้ง Python และไลบรารีที่เกี่ยวข้องดังนี้:

1. **ติดตั้ง Python**: ดาวน์โหลดและติดตั้ง Python (เวอร์ชัน 3.8 ขึ้นไป) จาก [python.org](https://www.python.org/)
2. **ติดตั้งไลบรารี**: เปิด Terminal หรือ Command Prompt แล้วพิมพ์คำสั่ง:
   ```bash
   pip install PySide6
   ```
3. **รันโปรแกรม**: พิมพ์คำสั่งเพื่อเปิดโปรแกรม:
   ```bash
   python main.py
   ```

## 1. การเริ่มต้นใช้งาน
1. เปิดโปรแกรมขึ้นมา หน้าจอหลักจะแสดงแท็บ **"จัดการข้อมูล"** เป็นหน้าแรก
2. หากมีไฟล์ข้อมูลเดิมอยู่แล้ว โปรแกรมจะโหลดขึ้นมาให้อัตโนมัติ
3. หากต้องการสร้างไฟล์ใหม่ ให้ไปที่เมนู **ไฟล์ (File) > สร้างไฟล์ใหม่** (หรือกด `Ctrl+N`)

## 2. การจัดการข้อมูลนักศึกษา (แท็บจัดการข้อมูล)
ในหน้านี้คุณสามารถ เพิ่ม, ลบ, แก้ไข และดูรายละเอียดของนักศึกษาได้

### 2.1 การเพิ่มนักศึกษาใหม่
1. คลิกปุ่ม **"+ เพิ่มนักศึกษา"** สีน้ำเงินด้านบน
2. กรอกข้อมูลในแบบฟอร์มให้ครบถ้วน (รหัสนักศึกษา, ชื่อ-นามสกุล, สถานที่ฝึกงาน ฯลฯ)
3. กดปุ่ม **"บันทึก"**

### 2.2 การแก้ไขข้อมูล
1. คลิกเลือกรายชื่อนักศึกษาจากตารางด้านซ้าย
2. รายละเอียดจะแสดงที่ด้านขวา
3. คลิกปุ่ม **"แก้ไขข้อมูล"**
4. แก้ไขข้อมูลตามต้องการ แล้วกด **"บันทึก"**

### 2.3 การลบข้อมูล
1. คลิกเลือกรายชื่อนักศึกษาที่ต้องการลบ
2. คลิกปุ่ม **"ลบข้อมูล"** สีแดง
3. กดยืนยันการลบ

## 3. การบันทึกความคืบหน้างาน (Tracking)
1. เลือกนักศึกษาที่ต้องการบันทึกงาน
2. คลิกปุ่ม **"บันทึก/ติดตามงาน"** สีเขียว
3. โปรแกรมจะพาไปที่หน้า **"บันทึกการติดตามงาน"**
4. ในหน้านี้จะมีรายการงานต่างๆ (เช่น รายงาน 2 เดือน, นิเทศ, โครงงาน)
   - **ติ๊กถูก** หน้าหัวข้อเมื่อนักศึกษาทำงานนั้นเสร็จแล้ว
   - เมื่อติ๊กถูกแล้ว จะสามารถ **เลือกเกรด** และ **กรอกหมายเหตุ** ได้
   - หากเอาติ๊กถูกออก ข้อมูลเกรดและหมายเหตุจะถูกล้างค่า
5. เมื่อบันทึกเสร็จแล้ว ให้กดปุ่ม **"บันทึกและย้อนกลับ"** ด้านบน หรือ **"บันทึกข้อมูล"** ด้านล่าง

## 4. การดูภาพรวม (แท็บภาพรวมความคืบหน้า)
คลิกที่แท็บ **"ภาพรวมความคืบหน้า"** เพื่อดูตารางสรุป
- ตารางจะแสดงรายชื่อนักศึกษาทุกคน พร้อมสถานะงานแต่ละชิ้น
- **สีของเกรด**:
  - 🟢 **A, B**: สีเขียว (ดีมาก/ดี)
  - 🟠 **C, D**: สีส้ม/เหลือง (ปานกลาง/พอใช้)
  - 🔴 **F**: สีแดง (ปรับปรุง)
- สามารถ **ดับเบิ้ลคลิก** ที่ช่องตารางเพื่อดูรายละเอียดเพิ่มเติมของงานชิ้นนั้นๆ ได้

## 5. การบันทึกและส่งออกไฟล์ (Export)
- **บันทึกไฟล์**: กด `Ctrl+S` หรือเมนู **ไฟล์ > บันทึก** เพื่อบันทึกข้อมูลลงไฟล์ JSON ปัจจุบัน
- **Export CSV**: เมนู **ไฟล์ > Export CSV** เพื่อนำข้อมูลออกไปเปิดใน Excel
- **Export PDF**: เมนู **ไฟล์ > Export PDF** เพื่อสร้างรายงานสรุปเป็นไฟล์ PDF

## 6. คีย์ลัด (Shortcuts)
- `Ctrl+N`: สร้างไฟล์ใหม่
- `Ctrl+O`: เปิดไฟล์
- `Ctrl+S`: บันทึกไฟล์
- `Ctrl+Shift+S`: บันทึกเป็น...
